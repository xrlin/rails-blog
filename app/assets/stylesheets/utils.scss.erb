@mixin content-bg {
  background: hsla(0,0%,100%,.3);
  overflow: hidden;
  border-radius: .3em;
  box-shadow: 0 0 0 1px hsla(0,0%,100%,.25) inset,
              0 .5em 1em rgba(0, 0, 0, 0.6);
  text-shadow: 0 1px 1px hsla(0,0%,100%,.3);
}

@mixin blur-glass {
  // background: url(<%= asset_path 'background.jpg' %>) no-repeat center center fixed;
  background: rgba(242, 242, 242, 0.73);;
  content: '';
  position: absolute;
  top: 0; right: 0; bottom: 0; left: 0;
  z-index: -1;
  -webkit-filter: blur(15px);
  -ms-filter: blur(15px);
  -o-filter: blur(15px);
  filter: blur(15px);
}
